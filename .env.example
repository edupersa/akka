# Copy to .env.local and fill in when ready to activate analytics/marketing.
# Both are optional — the site works fully without them, and the cookie
# consent banner already gates them behind user consent (see components/Analytics.tsx).

NEXT_PUBLIC_GA_ID=
NEXT_PUBLIC_META_PIXEL_ID=
